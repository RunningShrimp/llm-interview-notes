# 后端 → AI 应用工程师 · 面试知识总纲（2026 版）

> 定位：面向**后端开发**转型 AI 应用/平台工程的面试知识体系。
> 来源：综合目录下三份参考文档（1.md 阶段叙事版 / 2.md 工程深度版 / 3.md 十层全景版）+
> 2025-2026 考点检索（RAG 高频 28 题、
> 海外 LLM Engineering 面试指南）+ 面试官视角的判断。
> 原则：**不深入算法与数学推导**，一切以"工程判断力 + 系统设计 + 生产化"为核心。

---

## 〇、先说我的核心观点（备考总纲略）

### 观点 1：你的定位不是"转行"，而是"升维"
AI 应用工程师 ≈ **后端基本功 + LLM 应用架构 + 工程稳定性**。
业内共识：LLM 工程 **80% 是工程，20% 是 LLM**。你的后端经验（高并发、存储、
分布式、稳定性）不是要推倒重来，而是把成熟的工程判断力**映射**到 AI 系统的新瓶颈上：

| 传统后端的瓶颈 | AI 系统里对应的形态 |
|---|---|
| QPS / 连接数 | Token 吞吐 / RPM/TPM 配额 |
| 响应延迟 P99 | TTFT（首 token 延迟）/ 吞吐速率 |
| 数据库成本 | Token 成本 / 推理成本 |
| 缓存命中率 | 语义缓存命中率 / KV Cache |
| 接口确定性 | 概率性输出 → 需要校验、评测、兜底 |
| 服务降级 | 模型降级（大模型→小模型→规则兜底） |

面试中每一处都主动做这个映射，是你区别于"只会调 API 的年轻人"的核心武器。

### 观点 2：Junior 和 Senior 的分界线
- Junior：会用框架搭起来，Demo 能跑（LangChain + Milvus + 一个聊天界面）。
- Senior：**评测怎么建、失败怎么恢复、成本怎么控、上下文怎么管、效果怎么量化**。
  典型 Senior 回答范式："分层记忆系统 + 短期缓冲/长期向量检索 + 工具容错调度 +
  并发执行优化 30% 端到端延迟"。
- 面试官最反感：背框架名词（LangChain/LlamaIndex），讲不出框架背后裸 API 调用发生了什么。

### 观点 3：面试官的"四大追问"闭环（每类题都逃不掉）
1. **效果怎么量化？** —— 没有指标的回答等于零。
2. **失败了怎么办？** —— 检索为空 / 工具报错 / 模型超时 / 幻觉投诉，各自兜底方案。
3. **规模放大 10 倍会怎样？** —— 容量评估、瓶颈定位、水平扩展。
4. **为什么选这个方案？** —— 至少两个方案的对比 + trade-off（含成本）。

### 观点 4：2025-2026 高频考点已收敛（按考察频率排序）
1. RAG 检索质量（近两月最高频硬技能）
2. Agent 架构与 ReAct / Workflow vs Agent 选型
3. 上下文与记忆管理（Context Engineering，重点考察方向）
4. Function Calling / MCP / 工具安全
5. LLM 应用工程化：评测、降级、回滚、成本、延迟
6. 后端基础：数据库、限流、缓存、K8s（仍是地基）
7. 新热点：Agentic RAG、MCP vs Function Calling 区别、A2A、多智能体

---

## 知识体系总览（由浅入深 8 层）

```text
L1 后端基础（你的基本盘，做 AI 映射即可）
L2 LLM 应用基础（把模型当软件组件来理解）
L3 Prompt 与 Context 工程（把 prompt 当程序来设计）
L4 知识库与数据工程（RAG 的上限由数据质量决定）
L5 RAG 检索增强生成（面试主战场）
L6 Agent 构建与编排（从被动回答到主动执行）
L7 评测体系（区分 Demo 工程师与生产工程师的分水岭）
L8 可观测性 · 成本 · 安全 · 生产化（体现资深度）
+  L9 扩展热点与 AI 系统设计题（压轴加分项）
```

学习主线一句话：**后端映射 → 模型即组件 → prompt 即程序 → 数据定上限 →
检索定精度 → 编排定能力 → 评测定可信 → 生产定生死**。

### 📚 分层详解索引（小林coding 风格，一层一文件）

| 文件 | 内容 | 行数 | 核心心法 |
|------|------|------|----------|
| [01_L1_后端基础.md](1-基础盘/01_L1_后端基础.md) | 网络/数据库/Redis/MQ/分布式 + AI 场景映射 + 进阶篇 | 1178 | 后端经验做映射即可，不必重学 |
| [02_L2_LLM应用基础.md](2-LLM应用核心/02_L2_LLM应用基础.md) | Token/Context Window/API 工程化/SSE/推理性能/解码采样/微调工程 + 进阶篇 | 1108 | 模型即组件——懂接口特性、性能特征、坑 |
| [03_L3_Prompt与Context工程.md](2-LLM应用核心/03_L3_Prompt与Context工程.md) | Prompt 分层/结构化输出/上下文工程/推理范式 + 进阶篇 | 957 | Prompt≈代码、窗口≈内存、Few-Shot≈缓存热数据 |
| [04_L4_知识库与数据工程.md](2-LLM应用核心/04_L4_知识库与数据工程.md) | 解析管线/切块/元数据/异步入库流水线 + 进阶篇 | 834 | RAG 的上限不由模型决定，由数据质量决定 |
| [05_L5_RAG检索增强生成.md](2-LLM应用核心/05_L5_RAG检索增强生成.md) | 混合检索/Rerank/Agentic RAG/失效排查 + 进阶篇 | 1143 | 优化手段、失效模式、量化指标三件套 |
| [06_L6_Agent构建与编排.md](2-LLM应用核心/06_L6_Agent构建与编排.md) | Workflow vs Agent/MCP/多Agent/记忆/HITL + 进阶篇 | 1320 | 让不靠谱的 LLM 在业务流程里靠谱地工作 |
| [07_L7_评测体系.md](3-生产工程化/07_L7_评测体系.md) | 三层指标/LLM-as-Judge/trace 级评测/反馈闭环 + 进阶篇 | 864 | 你怎么证明它有效——最强区分度 |
| [08_L8_生产化_可观测_成本_安全.md](3-生产工程化/08_L8_生产化_可观测_成本_安全.md) | AI Trace/模型级联/语义缓存/注入防御/降级 + 进阶篇 | 1006 | 换个词汇表讲 老经验 |
| [09_L9_热点与AI系统设计.md](4-面试冲刺/09_L9_热点与AI系统设计.md) | 前沿热点/视频多模态流水线/四道系统设计 walkthrough/STAR-L + 进阶篇 | 1216 | 热点决定像不像，系统设计决定是不是 |

每层文件体例：本章导读（考察频率★ + 面试官想听什么 + ASCII 知识地图）→ 知识点详解（是什么→为什么→怎么做 + ASCII 图 + 对比表）→ 🎤 面试这样答（含追问）→ 章末速查表 + 自检清单。

**两大刷题专区**（与九层笔记互补，各自独立成目录）：

| 专区 | 入口 | 内容 | 行数 |
|------|------|------|------|
| 算法热题 100 | [00_热题100总览.md](5-算法热题100/00_热题100总览.md) | 17 组 100 道 LeetCode 热题，每题完整可编译 Go 代码 + 🎤 口述模板，按 4 阶段刷题顺序组织 | 9177 |
| 系统设计题 13 道 | [00_设计题总览.md](6-系统设计题/00_设计题总览.md) | 后端经典 5 题 + AI 工程化 8 题，考频按 2025-2026 检索校准，每题六步法 + 追问链 6-8 层 + 🎤 口述模板 | 4313 |

**全栈专属赛道**（[七、全栈专属赛道](7-全栈赛道/01_全栈转AI_优势与90天路线图.md)，与主线互补）：后端读者走 L1–L9 主线即可；全栈/前端读者加走第七章专属赛道三部曲——优势与 90 天路线图、生产级项目研读指南、全栈 AI 面试题精选，主线知识不变，按全栈视角补齐差异化优势、精读仓库与追问清单。

---

## L1 · 后端基础（快速复习 + AI 场景映射）

> 目标：不是重新学，而是每个点都能答出"原理 + 线上实战 + 在 AI 系统里怎么用"。

### 1.1 网络与协议
- HTTP/1.1 / 2 / 3、TLS、Keep-Alive、Chunked Transfer
- **AI 映射重点：SSE（Server-Sent Events）**——为什么 LLM 流式首选 SSE 而非 WebSocket
  （单向推送、HTTP/2 兼容、自动重连、网关友好）
- Nginx 对 SSE 的缓冲陷阱：`proxy_buffering off`，否则"假流式"
- 经典题："一个 HTTP 请求从浏览器到服务端返回，中间经历了什么？"

### 1.2 数据库（MySQL/PostgreSQL）
- B+Tree → 聚簇/二级/联合索引 → 最左匹配 → 覆盖索引 → ICP
- 事务与锁：MVCC、Redo/Undo/Binlog、WAL、四种隔离级别、Gap/Next-Key Lock
- 慢查询优化、分库分表
- **AI 映射**：RAG 元数据存储、对话历史表设计、向量库与关系库的混合架构（pgvector 何时够用）

### 1.3 缓存（Redis）
- 数据结构选型；缓存穿透/击穿/雪崩三件套的工程解法；热 key、大 key
- 分布式锁（SETNX / RedLock 的争议要能讲）
- **AI 映射**：语义缓存（相似问题命中缓存）、Token 配额计数器、会话状态存储

### 1.4 消息队列
- Kafka/RocketMQ/RabbitMQ；投递语义（At Most / At Least / Exactly Once 及为何工程难做到）
- 消息不丢、不重、顺序、积压、死信队列
- **AI 映射**：文档向量化入库流水线（削峰+异步+重试）、Agent 异步任务编排、
  LLM 长任务提交-回调模式

### 1.5 并发与分布式
- 进程/线程/协程（Go GMP / Python asyncio）；I/O 多路复用；并发≠并行、异步≠多线程
- RPC（gRPC/Protobuf）、注册发现、限流熔断降级、链路追踪
- CAP/BASE、分布式锁、幂等设计、分布式事务（Saga/TCC 概念级）
- **AI 映射**：基于 Token 而非 QPS 的限流；推理请求队列与优先级调度；模型网关熔断降级

### 1.6 系统设计思维框架
任何设计题（含 AI 设计题）固定从这些维度展开：
```text
数据量 → 并发量 → 延迟 → 一致性 → 可用性 → 容灾 → 成本 → 可观测
```

---

## L2 · LLM 应用基础（把模型当软件组件）

> 目标：不训练模型，但要知道它是个什么样的"组件"，有什么"接口特性"和"坑"。

### 2.1 核心概念
- Token / Context Window / Temperature / Top-P / Max Tokens
- Embedding / 相似度（余弦/点积/欧氏）——概念级即可
- 模型类型演进：Base → Instruction → Chat → **Reasoning 模型**（o1/R1 类，
  思考 token、延迟更高、prompt 方式不同）
- 训练概念只需名词级：预训练 / SFT / RLHF / DPO / LoRA / 量化（AWQ/GPTQ）

### 2.2 LLM API 工程化（后端主场）
- 鉴权、超时、重试（指数退避）、幂等
- **流式响应**：SSE 协议、断点续传、前端渐进渲染、异常中断处理
- **速率限制**：RPM/TPM 配额管理；embedding 接口限流 → 批量请求 + 缓存 + 本地模型兜底
- 多模型接入：OpenAI / Claude / GLM / DeepSeek 等 API 的差异与适配层设计

### 2.3 推理性能概念（不深入）
- TTFT / Prefill / Decode / tokens-per-sec；为什么 Prompt 越长越慢
- 为什么流式提升体验但不降低总成本
- KV Cache / PagedAttention（vLLM）——知道"能显著提升吞吐"即可
- Prompt Caching（前缀缓存）：system prompt 固定部分复用计费折扣

---

## L3 · Prompt 与 Context 工程

> 目标：把 prompt 当程序设计，把上下文当资源管理。这是 2025-2026
> 重点考察方向。

### 3.1 Prompt 工程化
- 结构分层：System（角色/策略）→ 任务定义 → 上下文 → 示例（Few-Shot）→ 用户输入 → 输出 Schema
- **Prompt 即代码**：版本管理、单元测试、回归测试、A/B 实验
- 防注入：用户输入与指令隔离（分隔符+角色标记）、输入过滤、输出校验

### 3.2 结构化输出与契约工程
- Function Calling / JSON Mode / JSON Schema 约束
- 运行时校验（Pydantic/Zod/Instructor）+ 非法输出自动重试（Self-Correction Loop）
- 下游解析器**永远不盲信**模型输出（面试金句）

### 3.3 Context Engineering（上下文工程）
核心心智模型：
```text
User Input → Context Builder（对话历史 + 用户画像 + 检索知识 + 工具结果 + 记忆 + 系统规则）→ LLM
```
- **上下文预算分配**：窗口快满时优先保留 系统指令 > 当前任务 > 关键约束 > 工具结果，压缩低价值历史
- **分层记忆**：最近几轮保留原文；较早历史 rolling summary；长期记忆（用户偏好/事实/结论）
  通过 相似度+时间+重要性 召回，不必每轮注入
- 多轮对话的指代消解（query rewrite 补全指代）

---

## L4 · 知识库与数据工程

> 观点：**RAG 的上限不由模型决定，而由数据质量决定**。这层的"脏活累活"最能体现实力。

### 4.1 文档解析管线
- 解析器选型：PyMuPDF（快）vs Marker/MinerU（学术/表格）vs OCR（扫描件）vs 多模态大模型（贵而准）
- 难点：表格、多栏、图片、页眉页脚；版面分析 + OCR + 坐标级切分
- 清洗：去重（SimHash/MinHash 概念级）、格式归一化、PII 脱敏
- **保留溯源信息**（页码/段落号）——引用与合规的基础

### 4.2 切块（Chunking）
- 策略：固定长度 / 递归分割（生产默认）/ 语义切块（召回率+~9%，但更贵）/ 结构感知（Markdown/标题层级/代码块）
- chunk size 与 overlap 的权衡：小而准 vs 大而全；overlap 过大 → 索引膨胀 + 重复上下文
- **Parent-Child（Small-to-Big）索引**：小块精准检索，命中后取父块提供完整上下文
- 高频题："为什么 chunk 不能越大越好？太小有什么问题？"

### 4.3 元数据工程与入库流水线
- Metadata 设计：来源、时间、版本、权限标签、实体、分类 → 检索时 Pre-filtering
  （权限过滤、时效过滤），大幅提升精度与速度
- **异步向量化入库流水线**：上传 → 解析（MQ）→ 切块 → 向量化（批量/限流）→ 入库 → 质量校验
- 增量更新：文件指纹判重、版本管理、删除同步（向量库与源库一致性）
- 海量文档：分区/分 collection、冷热分层

---

## L5 · RAG 检索增强生成（面试主战场）

> 打法：不只讲流程，要讲**优化手段、失效模式、量化指标**三件套。

### 5.1 基础架构（必须白板能画）
```text
Query → 改写/扩展 → [向量检索 + BM25 关键词检索] → 融合(RRF) → Rerank 精排
      → Top-K 上下文组装 → LLM 生成（带引用）→ 答案
```

### 5.2 检索核心
- Embedding 模型选型：BGE / GTE / E5 / Jina / OpenAI；多语言、领域适配；维度与成本权衡
- 向量库选型：Milvus（大规模/分布式）vs Qdrant（轻量/Rust）vs pgvector（已有 PG、中小规模）vs ES（已有 ES 团队）
- 索引概念：HNSW（内存换时间，中小规模）/ IVF（省内存大规模）/ PQ 压缩；参数（ef/M/nprobe）调优经验即可
- **混合检索**：BM25 补向量检索的短板（专有名词、型号、代码）；RRF 倒数排名融合 vs 加权融合
- **Rerank 精排**：Cross-Encoder 原理概念；延迟代价与优化（top-k 截取、批处理）；BGE-Reranker/Cohere
- 查询改写：query rewrite（指代消解）、HyDE、多查询扩展（RAG-Fusion）、子问题分解、Step-back

### 5.3 进阶架构（体现视野）
- **Agentic RAG**：RAG 降级为 Agent 的一个工具，由 Agent 动态决定 是否检索→怎么检索→结果够不够→是否二次检索（2025 高频新题）
- **自适应 RAG**：按查询类型路由（闲聊直答 / 事实精确检索 / 开放问题广度检索）
- **GraphRAG**：知识图谱+向量，适合多跳推理与关系密集场景；构建维护成本高，LightRAG 轻量替代
- **RAG vs 微调**：知识外挂（更新快、可溯源）vs 参数固化（改风格/格式）。
  业界惯例 **RAG-First**，微调打补丁。此题几乎必问，要能展开 4+ 维度对比（成本/时效/可解释/维护）

### 5.4 失效模式与排查（背下这个流程，面试直接用）
```text
问题 → Query 改写对不对？→ 切块是否腰斩关键信息？→ 向量召回了吗？
     → 融合排序对不对？→ Rerank 精排了吗？→ 上下文组装完整吗？
     → Prompt 约束够吗（"无答案就说不知道"）？→ 引用对齐了吗？
```
典型失效四类：内容缺失（切块/召回/覆盖）、排序错误（缺 rerank）、上下文整合失败、幻觉兜底缺失。
"检索结果互相矛盾"（knowledge conflict）：权威源优先 / 时效优先 / 显式呈现分歧 / 元数据过滤废弃文档。

### 5.5 工程化难点（高频追问）
- 10 万 → 1000 万文档怎么扩展？多租户与权限隔离怎么做？
- 文档更新/删除如何同步索引？入库失败的补偿？
- 幻觉三板斧：Prompt 硬约束 → Rerank 卡源头 → 强制引用溯源（+低置信拒答）
- RAG-Fusion/备用小模型兜底机制

---

## L6 · Agent 构建与编排

> 心法：Agent = LLM + Tools + State + Control Loop。
> 让"不靠谱的 LLM"在业务流程里"靠谱地工作"——这是考架构师思维和容错设计。

### 6.1 核心范式
- **Workflow vs Agent 选型**（必考）：流程确定 → Workflow（可预测、可测试、便宜）；
  需要动态决策 → Agent（灵活但贵且不稳）。反模式：为了炫技上 Agent。
  高频变体："什么问题不值得用 Agent？"——答案要点：低复杂度、高确定性、成本敏感场景。
- ReAct（推理-行动-观察循环）vs Plan-and-Execute（先规划后执行）vs Reflection（自我修正）
- 循环控制：最大轮次、Token 预算、超时、重复检测（防死循环——面试官最爱追问）

### 6.2 工具调用（Function Calling / Tool Use）
- 协议机制：模型输出结构化 JSON 调用意图 → 运行时执行 → 结果回填 → 继续推理
- **Tool Schema 设计哲学**：描述质量决定调用成功率；参数枚举/必填约束/Few-shot 嵌入
- 后端视角封装：REST API → Tool 的转换规范；**错误如何反馈给 LLM 让其自愈重试**
  （错误信息要"模型可读"，不是堆栈）
- 幂等性（LLM 重试不能重复扣款——Stripe 式追问）、超时、Dry-run、审计日志
- **MCP（Model Context Protocol）**：标准化工具生态（即插即用）；与 Function Calling 的
  区别是 2025 高频新题（FC 是模型能力接口，MCP 是工具接入的传输/发现标准协议层）

### 6.3 多 Agent 协作
- 拓扑：Supervisor（中心调度）/ Swarm（去中心化交接）/ 分层
- 何时值得多 Agent：任务真并行、角色需不同 system prompt/工具集；协调开销是真实成本
- A2A（Agent-to-Agent）通信协议：身份互信、消息协议（概念了解即可）
- 死锁检测、超时熔断、部分失败处理（retry/backoff、fallback 工具、断路器、优雅降级返回部分结果）

### 6.4 记忆系统
- 短期：上下文窗口管理（滑窗、摘要压缩）
- 长期：向量库/KV 持久化；写入触发条件、检索（相似度+时间+重要性）、遗忘/合并
- 程序性记忆：SOP/Workflow 模板沉淀

### 6.5 可靠性与人机协同（Senior 分水岭）
- 失败模式清单：死循环、幻觉工具调用、跨步骤上下文漂移、越权操作、成本失控
- **Human-in-the-Loop**：不可逆操作（发邮件/删除/支付）必须人工确认；
  审批流异步中断 + Checkpoint 状态保存 + 超时策略
- 确定性保障：规则引擎嵌概率系统、关键输出强校验、Guardrails 输入输出过滤
- Agent 权限：专用身份（非共享凭据）、RBAC 最小权限、爆炸半径控制

---

## L7 · 评测体系（最强区分度）

> 观点：这是有生产经验者形成区分度的板块，也是多数人的空白。
> 核心句式："**你怎么证明它有效？**"——答不出指标体系的都是 Demo 工程师。

### 7.1 RAG 评测（三层模型）
```text
检索层：Hit Rate / MRR / NDCG / Context Precision / Context Recall
生成层：Faithfulness（忠实度）/ Answer Relevancy / 正确性 / 幻觉率
系统层：延迟 / Token 成本 / 错误率 / 吞吐
```
- 工具：RAGAS / TruLens / DeepEval（LLM-as-a-Judge 自动化）
- **LLM-as-a-Judge 的坑**：位置偏差、长度偏差、自我偏好 → 校准方法
  （与人工标注对齐、多裁判投票、评分标准 rubric 固化）——高级加分点
- 检索指标可离线精确计算（有标准答案时），生成指标依赖 judge 或人工

### 7.2 Agent 评测（trace 级）
- 结果指标：任务完成率、最终答案质量
- **过程指标（Trajectory）**：工具选择准确率、参数正确率、步骤效率、错误恢复率
- 关键认知：**最终答案对 ≠ 过程对**（可能乱调工具碰巧对了）→ 必须 trace-level evaluation
- Agent 评测框架：RAGAS agent 指标（Tool Call Accuracy/F1、Goal Accuracy）、LangSmith evals
- 端到端：延迟分解、每任务成本

### 7.3 数据集与反馈闭环
- 黄金数据集构建：人工标注 vs LLM 合成 vs **线上日志挖掘**（最优）；边界 case 覆盖；版本管理
- **在线反馈闭环**：点赞/复制/追问/改写等隐式信号 → badcase 自动入库 → 回归测试集 → 迭代
- 离线指标与在线 A/B 的关联；灰度发布
- "5% 幻觉投诉，一周内降到 0.5% 怎么做？"——定位（badcase 归因：检索层 or 生成层）
  → 分层修复 → 回归验证 → 监控告警

---

## L8 · 可观测性 · 成本 · 安全 · 生产化

### 8.1 AI 可观测性（与传统后端高度复用）
- AI Trace：Retrieval → Rerank → Generation → ToolCall 的 Span 层级
  （LangSmith / Langfuse / OpenTelemetry GenAI 语义约定）
- 记录：Prompt 模板版本、模型版本、输入输出、Token 用量、延迟分解（TTFT/TBT）
- 采样监控线上输出质量；敏感词与合规拦截；隐私脱敏日志
- 告警：成本异常、幻觉率上升（抽检 judge）、工具失败率

### 8.2 成本与性能优化
- **模型级联/路由**：小模型分类 → 简单任务便宜模型，复杂才升大模型（2025 面试必考点）
- 缓存分层：精确匹配 → 语义缓存（相似度阈值权衡：命中率 vs 幻觉风险）→ 模型推理
- Prompt 压缩（LLMLingua）、历史摘要化、Batch API、Prompt Caching
- 高并发推理：请求队列、优先级调度、超时丢弃、排队体验设计

### 8.3 安全与合规
- **Prompt Injection 纵深防御**：输入过滤、指令与数据隔离、输出校验、Canary Token、
  最小权限工具（RAG 检索结果本身可能藏注入——间接注入）
- 数据：PII 检测脱敏、数据驻留、审计日志、引用溯源（版权/合规）
- Agent 特有：越权操作、数据外泄、工具滥用 → 沙箱（Docker/gVisor/E2B）、白名单、配额

### 8.4 部署与稳定性
- 自建 vs API：vLLM/TGI 私有化部署概念；量化模型的精度评估
- 降级链路：主模型超时 → 备用模型 → 规则/缓存兜底，且保持上下文一致
- 回滚：Prompt/模型版本化，一键回退；灰度
- SLA 与容量评估：LLM 是计算密集+长耗时任务，容量模型与传统 QPS 思维完全不同

---

## L9 · 扩展热点与 AI 系统设计题（压轴）

### 9.1 前沿热点（概念清晰即可，展示视野）
- MCP 生态与工具标准化；A2A 多 Agent 互信
- Agentic RAG / Self-RAG / 多模态 RAG（图文档问答）
- 推理模型（o1/R1 类）对应用架构的影响（更长延迟、更高成本、更强规划）
- Fine-tuning 落地决策框架：什么时候 RAG 不够（风格/格式/领域术语大量注入）

### 9.2 AI 系统设计题（高级岗压轴，用固定框架答）
答题框架：
```text
① 需求澄清（用户量/QPS/延迟/准确率/预算/合规）
② 能力拆解（Plan / Tools / Memory / Knowledge / Evaluator 五模块）
③ 数据流图 + 模块时序（白板画）
④ 容量评估 → 瓶颈 → 降级/熔断/灰度
⑤ 成本-效果折衷（模型选型、缓存、级联）
⑥ 评测与监控怎么建
```
高频题型：
- 设计企业知识库问答（10 万 → 1000 万文档、权限、多租户、增量更新、评测）
- 设计智能客服 Agent（意图路由、工具链、HITL 转人工、幻觉投诉兜底）
- 万级并发 Agent 服务平台（水平扩展、热更新 Prompt、灰度、成本控制）
- 多模态客服（截图+文本的意图链设计）

---

## 十、备战策略（给后端的行动指南）

### 10.1 时间分配（我的权重建议）
| 模块 | 权重 | 目标 |
|---|---|---|
| 后端基础 | ★★★ | AI 场景映射即可，不重学 |
| MySQL / Redis / MQ / 分布式 | ★★★★ | 原理+线上案例，维持深度 |
| 系统设计（含 AI 设计题） | ★★★★★ | 核心竞争力——L9 入门四题打框架，[13 道专区](6-系统设计题/00_设计题总览.md)练深度 |
| LLM 基础 + Prompt/Context | ★★★★ | 能工程化 |
| 知识库 + RAG | ★★★★★ | AI 岗核心，深挖失效模式 |
| Agent 构建与选型 | ★★★★★ | 架构+容错+安全 |
| **评测体系** | ★★★★★ | 最强区分度，重点投入 |
| 可观测/成本/安全 | ★★★★ | 生产化能力 |
| OJ 算法手撕（[热题 100 专区](5-算法热题100/00_热题100总览.md)） | ★★★ | 目标公司有 OJ 轮时升为 ★★★★★；无 OJ 轮维持手感即可 |
| 深度算法/数学 | ★ | 明确放弃 |

### 10.2 简历与项目包装
- ❌ "使用 LangChain 搭建了 RAG 系统"
- ✅ "设计生产级 RAG：BM25+向量+Rerank 三路管线，Parent-Child 索引，语义缓存降本 35%，
  Faithfulness 0.65→0.88，P99 < 2s"
- 公式：**技术方案 + 量化指标 + 解决的工程难题 + 业务价值**

### 10.3 准备 3 个 STAR-L 深度项目故事
每个必须含：背景挑战(S) → 职责目标(T) → **至少两方案对比+选型理由+踩坑**(A) →
量化结果(R) → 复盘与方法论沉淀(L)。

### 10.4 高频自测清单（逐条过）
1. 你的 RAG 什么情况下彻底失效？兜底方案？
2. RAG 效果不好，怎么系统性排查？（背 5.4 流程）
3. 为什么加 BM25？为什么要 Rerank？怎么证明变好了？
4. RAG 和微调怎么选？Agentic RAG 和普通 RAG 区别？
5. 上下文窗口满了怎么办？长期记忆每轮都注入吗？
6. Workflow 和 Agent 怎么选？Agent 什么时候该停？
7. MCP 和 Function Calling 区别？
8. Agent 工具调用失败怎么反馈重试？LLM 重试怎么保证幂等？
9. 评测集怎么建？LLM-as-a-Judge 不可信怎么办？
10. LLM 延迟突增 3 倍，如何优雅降级？
11. 流量放大 10 倍，瓶颈在哪？
12. 幻觉投诉 5%→0.5%，一周内怎么落地？
13. 向量库为什么选 Milvus/pgvector？依据是什么？
14. 多轮对话指代消解怎么做？
15. 检索结果互相矛盾，系统怎么决策？

### 10.5 我的最后建议
- **写一个小而真的项目**（哪怕周末级）：自己解析文档→切块→混合检索→rerank→评测集跑分。
  一个真实 GitHub 项目 > 背 50 道题。
- 每个知识点按"原理一句话 → 方案对比 → 失败模式 → 量化指标"四段式组织答案。
- 练白板画架构：Agent 链路图、RAG 管线图、降级链路图各画熟一张。
- 模拟面试录音回放，检查是否"过于理论、没有工程细节"。

---

## 附：本大纲与参考文档的关系
- 1.md：阶段叙事可作速览；
- 2.md：工程深度细节（索引参数、沙箱、量化等）作 L4-L8 的展开词典；
- 3.md：十层全景可作知识点查漏清单；
- 本文件：**总入口 + 优先级 + 面试导向的组织方式**，以 2025-2026 高频考点校准，
  备考顺序和深浅以本文件为准。
